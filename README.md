# order-bot

New Telegram bot project. Agent workflow starts with **grill me** (`.agents/skills/grill-me`).

Ready-made templates and agent docs live in the sibling repo **gift-bot** — see `AGENTS.md` and `.cursor/rules/gift-bot-templates.mdc`.
