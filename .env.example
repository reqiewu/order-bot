# Telegram
TELEGRAM_BOT_TOKEN=
OPERATOR_TELEGRAM_ID=

# Markets (from gift-bot style credentials — do not commit real values)
MRKT_TOKEN=
PORTALS_TMA=

# Runtime (в Docker BOLT_PATH переопределяется на /data/order-bot.db)
BOLT_PATH=data/order-bot.db
POLL_INTERVAL_SEC=90
LOG_DEBUG=1

# Optional bootstrap watch slots (JSON array). Example:
# WATCH_SLOTS_JSON=[{"collection":"Lunar Snake","model":"Albino"}]
WATCH_SLOTS_JSON=
